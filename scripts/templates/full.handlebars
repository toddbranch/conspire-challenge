<div class="row">
    <div class="col-sm-2">
        <img src="{{image}}" width=80>
    </div>
    <div class="col-sm-10">
        <h4 class="modal-title">{{name}}</h4>
        <b>Addresses:</b>
        <ul class="addresses">
        {{#each addresses}}
            <li>
                {{this}}
            </li>
        {{/each}}
        </ul>
    </div>
</div>
