---
language: node_js
before-install:
  - "npm install -g grunt-cli"
  - "npm install -g bower"

install:
  - "npm install"
  - "bower install"

cache:
  directories:
    - node_modules
    - bower_components

script:
  - grunt test
